cd cyber-playbook/quantum-anomaly/app
pip install -r requirements.txt

Terminal 1 — Start API
  uvicorn api:app --reload --port 8003

Terminal 2 — Start Streamlit UI
  streamlit run main.py

  http://localhost:8501
